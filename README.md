# weblate-utils
Basically a small script to help you to interact with Weblate through API!
---

1. Use `uv sync` to install the dependencies. 
2. Set the API Key in your ENV.
3. run `python [file].py` :D